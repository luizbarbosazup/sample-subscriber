micronaut:
  application:
    name: sampleSubscriber
  server:
    port: 8081
cassandra:
  default:
    clusterName: '"myCluster"'
    contactPoint: '"localhost"'
    port: 9042
    maxSchemaAgreementWaitSeconds: 20
    ssl: true
nats.addresses:
- nats://localhost:4222
